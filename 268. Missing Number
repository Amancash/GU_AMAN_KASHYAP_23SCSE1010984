class Solution {
    public int missingNumber(int[] nums) {
        int no=nums.length;
        Arrays.sort(nums);
        for(int i =0 ;i<nums.length ;i++){
            if(i!=nums[i]){
                no=i;
                break;
            }
        }
        return no;
    }
}
